Int lines = 0;
Int sum = 0;
Stream newStream = [[0,0]];
while (lines < stream.size()){
	Int sum = (stream.getElem(0,lines)+(3*stream.getElem(1,lines)));
	newStream.add([sum]);
	Int lines = (lines + 1)
};
newStream.delete(0)
